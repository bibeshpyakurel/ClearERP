# SPA fallback for Cloudflare Pages.
# React Router owns client-side routing, so every unmatched path must serve
# index.html instead of returning a 404. This replaces nginx's `try_files`
# directive, which only applies to the local Docker Compose setup.
/*    /index.html   200
